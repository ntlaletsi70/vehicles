# Use the latest 2.1 version of CircleCI pipeline process engine.
# See: https://circleci.com/docs/2.0/configuration-reference
version: 2.1
orbs: 
  docker: circleci/docker@2.0.2
  kubernetes: circleci/kubernetes@1.3.0
# Define a job to be invoked later in a workflow.
# See: https://circleci.com/docs/2.0/configuration-reference/#jobs
jobs:
 build:
   machine: true
   steps:
     - checkout
     # start proprietary DB using private Docker image
     # with credentials stored in the UI
     - run: |
         echo "$DOCKER_PASS" | docker login --username $DOCKER_USER --password-stdin

       # build the  image
     - run: docker build -t ntlaletsi70/laravel:$CIRCLE_BRANCH .
    
     # deploy the image
     - run: docker push ntlaletsi70/laravel:$CIRCLE_BRANCH
     - run: git clone git@bitbucket.org:ntlaletsi/k8s.git
     - run: kubectl set image deployment/php_deployment php=ntlaletsi70/laravel:$CIRCLE_BRANCH

# Invoke jobs via workflows
# See: https://circleci.com/docs/2.0/configuration-reference/#workflows
workflows:
  build-workflow:
    jobs:
      - build
